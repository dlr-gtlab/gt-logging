# - Config file for the GTlabLogging package
# It defines the following variables
#  GTlabLogging_INCLUDE_DIRS - include directories for TiXI
#  GTlabLogging_LIBRARIES    - libraries to link against

include(CMakeFindDependencyMacro)

# Our library dependencies (contains definitions for IMPORTED targets)
if(NOT TARGET GTlabLogging)
  include("${CMAKE_CURRENT_LIST_DIR}/GTlabLogging-targets.cmake")
endif()

 
# These are IMPORTED targets created by GTlabLogging-targets.cmake
set(GTlabLogging_LIBRARIES GTlabLogging)
get_target_property(GTlabLogging_INCLUDE_DIRS GTlabLogging INTERFACE_INCLUDE_DIRECTORIES)

# Create an alias
add_library(GTlab::Logging INTERFACE IMPORTED)
set_target_properties(GTlab::Logging PROPERTIES
  INTERFACE_LINK_LIBRARIES "GTlabLogging"
)
